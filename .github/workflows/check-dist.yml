# Disabled until TS bundling is reintroduced
name: Check Transpiled JavaScript (disabled)

on:
  # Manual-only while the project is plain JS. Re-enable PR/push triggers on TS migration.
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check-dist:
    name: Check dist/ (disabled)
    if: ${{ false }} # Temporarily disabled; remove when TS build/bundle is restored
    runs-on: ubuntu-latest
    steps:
      - run: echo "check-dist workflow is disabled until TypeScript bundling is restored"


